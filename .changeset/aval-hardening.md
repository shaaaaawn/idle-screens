---
"@idle-screens/core": minor
"@idle-screens/schema": minor
"@idle-screens/savers-classic": minor
---

Host-owned fallback slot on <idle-screen> (slot="fallback" shows when a saver fails to mount); published SaverSpec JSON Schema + FORMAT.md format spec with @idle-screens/schema; mystify saver ships in savers-classic
