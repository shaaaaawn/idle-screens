node_modules/
dist/
coverage/
*.tsbuildinfo

# playwright
test-results/
playwright-report/
blob-report/
playwright/.cache/

# vite
apps/*/dist/
.vite/

# swift / macOS app
.build/
*.dmg
apps/mac/.secrets/

# os / editor
.DS_Store
*.log

# Never commit npm auth
.npmrc


.mcp.json
.env

# Agent worktrees — local scratch, never commit.
# Scoped to worktrees/ on purpose: .claude/launch.json and .claude/skills/ are
# tracked intentionally and ship with the repo.
.claude/worktrees/